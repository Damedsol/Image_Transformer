# Pipeline Checkpoint — imageTransformer

# Layer 1 = the CURRENT cycle (top level). Layer 2 = previously closed cycles,
# nested under `closed_cycles`. Never let the same key exist at both layers:
# duplicate YAML keys are silently shadowed (last one wins) — that bit us twice.

pipeline:
  status: "completed" # plan_completed | build_completed | reviewer_approved | reviewer_rejected | completed
  cycle: "2026-09-17 release-2.1.0 documentation corrections (post-release, harness-only)"
  plan_file: null
  plan_note: "No plan and no change_spec: harness documentation correction. Trigger: finishing release 2.1.0 exposed an error in the harness docs (the spec claimed the repository has no tags). No production code and no test changed, so TDD is N/A; the evidence is the git-flow source lines cited plus the measured repo state."
  spec_file: ".agents/specs/change_spec.yaml"
  change_spec: ".agents/specs/change_spec.yaml (status: archived — belongs to the closed dependency cycle, untouched)"
  review_file: ".agents/docs/review_2026-09-17_release-docs-correction-repass.md"
  review_prev_file: ".agents/docs/review_2026-09-17_release-docs-correction.md"
  reviewer_pass: "Pass 1 REJECTED (ID-01/ID-02) → Pass 2 APPROVED after the fixes. The `reviewer_evidence`/`reviewer_verdict` fields describe pass 1; the `_pass2` fields describe this approval."
  reviewer_evidence_pass2: "ID-01 verified: git show-ref --tags | wc -l → 20, the spec now states '20 tags as of 2026-09-17' (self-dated), and the four sites (spec, checkpoint evidence, build_state evidence.tags, history) all say 20; the only remaining '21 tags' text is the frozen pass-1 finding and the history lesson that quotes it. ID-02 verified: no volatile ahead-of-origin figures remain in any instruction — checkpoint.yml and build_state.json both tell the user to measure with git rev-list --count; the build also fixed an extra occurrence in build_state.json that the pass-1 finding had not cited (same ID). Non-regression: the tagging section stays coherent and the release state is unchanged (tag V2.1.0 → 70d54fda → commit c5ae925, release/2.1.0 deleted, main/develop carriers identical). The pass-1 review artifact was not touched by the build (mtime 20:23:18 vs the build edits at 20:24:47+)."
  reviewer_verdict_pass2: "APPROVED — 0 KEEP / 0 REMOVE (documentation-only cycle); suite green (22 files / 161 tests); biome check-only 0; workload 4 files / 0 code-config lines."
  reviewed_at: "2026-09-17"
  reviewer_hash: "29ca7c8765c76c3b"
  reviewer_hash_note: "sha256(build_hash + \"\\n\" + diff_stat)[0:16] using the PASS-2 values of those fields, read from this file as /build left them. Reviewed-surface hash of the frozen candidate (.agents diff only): 74221b02a44d253b852fcbd88e513c0a9252fcad7c4be491ba73fe339827435f (pass 1 was 25b8b8c4…)."
  reviewer_evidence: "Affirmation-by-affirmation check: gitflow.prefix.versiontag = V (git config); V1.0.0/V2.0.0/V2.1.0 are annotated tag objects; V2.1.0 -> object 70d54fda -> commit c5ae925; V2.0.1 absent; release/2.1.0 deleted; git diff main develop -- <5 carriers> empty (both at 2.1.0); git-flow-release:817 require_clean_working_tree unconditional inside finish while :571 (gitflow.allowdirty) belongs to the start handler; :872/:768/:769 the annotated tag and its -m/-f flags; :844/:859 idempotent skip of an already-merged branch and an existing tag; gitflow-common:758-759 echo \"$@\" explains the duplicated annotation V2.1.0 V2.1.0; origin/main..main = 9 commits. Two counts were wrong: tags are 20, not 21 (ID-01) and develop is 12 ahead of origin/develop, not 6 (ID-02)."
  reviewer_verdict: "REJECTED — ID-01 (blocking: false tag count 21 vs 20, four occurrences) and ID-02 (blocking: stale and volatile ahead-count). No tests to remove (docs-only cycle). Suite green, no /debug needed. Everything else in the cycle verified correct."
  build_hash: "docs-corrections-pass2@70d54fda: ID-01 the tag count is 20 not 21 (four places; the spec figure is now self-dated) + ID-02 the volatile ahead-of-origin counts were dropped from pending_ids"
  build_state: ".agents/.state/build_state.json"
  diff_stat: ".agents only — release-versioning.md (tag count 21 → 20 with an as-of date), checkpoint.yml (evidence count + pending_ids cleanup + new build_hash/diff_stat), build_state.json (evidence.tags count + this pass), history.md (count + the review-fix lesson). Still no code, no tests."
  built_at: "2026-09-17"
  last_step: "scribe"
  scribe_hash: "rev:29ca7c8765c76c3b+spec:release-versioning.md#A+V1+tagging"
  scribe_hash_note: "Readable composite key (same convention as the three previous cycles): this repo has no domain.yaml and the /scribe sandbox exposes no hashing utility. It differs from every closed cycle's key, so this cycle was genuinely pending archive, not a repeated run."
  completed_at: "2026-09-17T20:27:39+01:00"
  completed_at_note: "ISO timestamp of this gate's mem_save (session id 53) — `date` is unavailable inside the /scribe sandbox."
  adr_file: null
  adr_note: "No ADR: nothing architectural changed. The release-tagging conventions are durable config knowledge (memory 52) plus .agents/docs/specs/release-versioning.md."
  archive:
    spec: ".agents/docs/specs/release-versioning.md — the 'Tagging and completion' section was corrected in place (20 tags self-dated, the V2.0.1 gap, the finish requirements: -m without a TTY, clean tree, idempotency, --theirs recovery). This cycle had NO change_spec.yaml deltas; the archived change_spec of the dependency cycle was left untouched."
    adr: null
    adr_note: "n/a — the release procedure is documented in the living spec plus memory 52 config/release-tagging; no architectural decision was taken."
    reviews: [".agents/docs/review_2026-09-17_release-docs-correction.md (pass 1: REJECTED — ID-01/ID-02)", ".agents/docs/review_2026-09-17_release-docs-correction-repass.md (pass 2: APPROVED)"]
    memory_ids: [53]
    memory_note: "memory 52 (config/release-tagging) was updated earlier in this session to revision 2 with the git-flow findings."
    index_project: "projects-github-imagetransformer (reindexed: 131 → 133 files)"
    history: ".agents/context/history.md (entry 2026-09-17: release 2.1.0 + the review-fix pass + QA)"
    commit: "PENDING — see pending_ids: the /scribe sandbox blocks git add/commit, so the user runs the paste-safe block"
  evidence: "Claim-by-claim verification of every corrected statement: `git show-ref --tags` → 20 tags, all V-prefixed, V2.1.0 present, V2.0.1 absent; `git show V2.1.0 --no-patch` → annotated tag whose commit is the merge c5ae925, annotation body 'V2.1.0 V2.1.0' (explained by /usr/bin/gitflow-common:758-759, the run_filter_hook fallback `echo \"$@\"`, which appends the tag name to the message); /usr/bin/git-flow-release:872 (annotated tag, `-m` used only when supplied), :817 (`require_clean_working_tree`, unconditional), :844 and :859 (skips an already-merged branch and an existing tag), :571 (`gitflow.allowdirty` honoured only by start); `git diff main develop -- <5 carriers>` → empty (main and develop both at 2.1.0); `pnpm test` → 22 files / 161 tests green on develop."
  not_run:
    - "pnpm qa as a whole — AGENTS.md no-auto-runs policy. `biome format .` was run read-only (check mode, no --write) to confirm the .md/.json edits stay biome-clean."
  pending_ids:
    - "RESOLVED in this pass (pass 2): ID-01 → 20 tags in all four places (release-versioning.md, checkpoint.yml evidence, build_state.json evidence.tags, history.md), with the spec's figure now self-dated so it cannot silently rot; ID-02 → the volatile ahead-of-origin counts were removed from the push item"
    - "SCRIBE (after approval): commit the harness correction as docs(agents) — the /scribe sandbox refuses git add/commit in this environment, so the user executes the paste-safe commands"
    - "USER: push main + develop + tags (both branches carry unpushed commits — measure with `git rev-list --count origin/<branch>..<branch>`; point-in-time counts are deliberately not recorded here because they go stale immediately)"
    - "USER (optional): tag the untagged 2.0.1 release merge — `git tag -a V2.0.1 54ffdad -m 'V2.0.1'`"
    - "USER EXTERNAL (carried over): a stale foreign dev server still holds :5173 — kill it (with strictPort the failure is explicit instead of a silent drift)"
    - "OBS-02 (pre-existing, unverified in-container): docker-compose.yml:55 `pnpm run dev -- --host 0.0.0.0` never reaches Vite (concurrently swallows the flag; child argv [] in all 3 shapes). With vite `host: 'localhost'` the published 5173:5173 likely cannot reach the container frontend. Confirm with `docker compose up`."
    - "OBS-01 (DRY, pre-existing): backend/src/middlewares/securityMiddleware.ts:29-47 re-implements the CORS_ORIGIN/CORS_ORIGINS parsing now encapsulated in normalizeOrigin, and does not filter non-http(s) schemes for connectSrc"
    - "USER EXTERNAL (carried over): Dependabot alert export (no gh CLI / private repo / no auth)"
    - "USER DECISION (carried over): .github/workflows/security.yml pending the user's OK (introduces GitHub Actions for the first time)"
    - "DEFERRED (carried over): slice C2b dev tooling (biome, commitlint, concurrently, globals, oxlint, vite majors)"
    - "DEFERRED (carried over): slice C3 — check:security script, scripts/scan-advisories.mjs (ADR-0005 partially covered)"
    - "USER DECISION (carried over): close 11 stale origin/dependabot refs — commands in .agents/docs/evidence/advisory_triage_2026-09-17.md"
    - "POST-MERGE WATCH (carried over): first Dependabot run must not turn `catalog:` into an inline version nor an exact pin into a caret"
  open_questions:
    - "Q1 (carried over): where are the advisories seen (GitHub Dependabot UI / email / pnpm audit)? Needed to close the triage."
    - "Q2 (carried over): GitHub vs Gitea as the canonical CI host (.github/** vs other)."
  workload:
    files: 4
    scope: ".agents/ only (spec, checkpoint, build_state, history) — no code, no tests"
    note: "Excluded from the code/config SLO by the convention used in the previous three cycles; declared here for transparency."

  # ── Layer 2: previously closed cycles, preserved verbatim ──────────────────
  closed_cycles:
    "2026-09-17_release-2.1.0":
      status: "completed"
      summary: "Released 2.1.0: five version carriers synced and guarded by versionConsistency.test.ts; finished with git-flow as tag V2.1.0."
      plan_file: null
      plan_note: "Ad-hoc release chore handed over from /scribe (the carriers are code/config, outside that gate's write scope)."
      review_file: ".agents/docs/review_2026-09-17_release-2.1.0.md"
      reviewed_at: "2026-09-17"
      reviewer_hash: "12c389674c6881d1"
      reviewer_hash_note: "sha256(build_hash + \"\\n\" + diff_stat)[0:16]. Reviewer-surface hash of the frozen candidate: 41482c9542bcdb76463c7fc348ffd5522e7c3f4041aa516c7ce0eb5cac71e4d1."
      reviewer_verdict: "APPROVED — 4 KEEP / 0 REMOVE; workload 5 code/config files / 119 lines (inside SLO); 2 informational observations."
      reviewer_evidence: "The 6 version points were extracted with the reviewer's own patterns (all 2.1.0), no 2.0.0 residue outside the guard fixtures, the dev compose carries no versioned tag (no sixth carrier), and the guard CARRIERS match the version-bearing files of the 2.0.0 release (f128550) minus the history log. Suite: pnpm test 22 files / 161 tests, type-check 0, oxlint root+backend check mode 0, lint-filenames 0, biome format check-only 0."
      superseded: "The review's §6 claim that the repository 'has no tags' was wrong; it is superseded by the documentation-correction cycle above (the review artifact itself stays frozen as evidence)."
      scribe_hash: "rev:12c389674c6881d1+spec:release-versioning.md#A+V1"
      completed_at: "2026-09-17T20:04:33+01:00"
      completed_at_note: "ISO timestamp of that gate's mem_save (session id 50) — `date` is unavailable inside the /scribe sandbox."
      commit_status: "RESOLVED — committed as fc9f075 (chore: bump version 2.1.0) and 022c13e (docs(agents): archive the 2.1.0 release cycle), merged into main as c5ae925 (conflicts on the five carriers resolved with `git checkout --theirs`), then finished: tag V2.1.0 (object 70d54fda → commit c5ae925), back-merge into develop, release/2.1.0 deleted. The /scribe sandbox could not run git add/commit; the user executed the paste-safe commands."
      tag_status: "Tag V2.1.0 created (annotated). Known gap: V2.0.1 was never tagged — the 2.0.1 release was merged into main (54ffdad) but never finished."
      archive:
        spec: ".agents/docs/specs/release-versioning.md §A (V1) — corrected by the following cycle (tagging/completion section)"
        adr: null
        review: ".agents/docs/review_2026-09-17_release-2.1.0.md"
        memory_ids: [50, 51]
        memory_topic_keys: ["config/version-carriers"]
        config_memory_ids: [52]
        index_project: "projects-github-imagetransformer (reindexed: 127 → 131 files)"
        history: ".agents/context/history.md (release 2.1.0 entry)"
      workload:
        code_config_files: 5
        code_config_lines: 119
        note: "5 carrier lines + the 114-line guard test."

    "2026-09-17_dev-port-fallback":
      status: "completed"
      summary: "Reported `pnpm dev` CORS outage + HMR websocket failure: a foreign process held :5173, Vite drifted to :5174, and two hardcoded 5173 assumptions broke. Fixed with loopback CORS patterns in dev (exact list kept in prod, fail-loud when empty) and Vite strictPort with no pinned hmr.clientPort."
      plan_file: null
      plan_note: "Ad-hoc Fast Path: user-reported dev bug, small and single-concern (AGENTS.md §4.1). R1..R3 were declared in the test docblocks and archived as the living spec's section A."
      review_file: ".agents/docs/review_2026-09-17_dev-port-fallback.md"
      reviewed_at: "2026-09-17"
      reviewer_hash: "5c53fd73e39b130a"
      reviewer_hash_note: "sha256(build_hash + \"\\n\" + diff_stat)[0:16]."
      reviewer_verdict: "APPROVED — 11 KEEP / 0 REMOVE; workload 5 files / 385 code+config lines (inside SLO)."
      reviewer_evidence: "dev :3099 echoed Access-Control-Allow-Origin for http://localhost:5174, 127.0.0.1:5180 and [::1]:5174 plus a successful preflight; prod :3098 served only its exact origin; prod with an empty allowlist exited 1 before listening; Vite strictPort exited 1 with 'Port 5173 is already in use'; the served @vite/client shipped hmrPort = null (page-port fallback). Negative control: the pre-existing backend on :3001 reproduced the missing ACAO."
      scribe_hash: "rev:5c53fd73e39b130a+spec:dev-environment.md#A+D1-D3"
      completed_at: "2026-09-17T19:36:29+01:00"
      completed_at_note: "ISO timestamp of that gate's mem_save (session id 46) — `date` is unavailable inside the /scribe sandbox."
      commit_status: "RESOLVED — committed as cc26502 (fix(dev-env), 5 files 311+/74-) and 76ae2db (docs(agents), 7 files), then merged into develop as e8b892f. The /scribe sandbox could not run git add/commit (see .agents/context/error.log); the user executed the two paste-safe commands."
      cycle_date_note: "OBS-03: /build labelled this cycle 2026-09-18; the real system date was 2026-09-17 (date -I, file mtimes, and the previous cycle closing at 16:29 the same day). Normalised in checkpoint.yml, history.md and build_state.json."
      archive:
        spec: ".agents/docs/specs/dev-environment.md §A (D1..D3) — new living spec; no change_spec.yaml existed because this was an ad-hoc Fast Path cycle"
        adr: ".agents/docs/adr/0006-dev-origin-allowlist-and-port-binding.md"
        review: ".agents/docs/review_2026-09-17_dev-port-fallback.md"
        memory_ids: [45, 46, 47]
        memory_topic_keys: ["architecture/dev-origin-allowlist", "config/concurrently-arg-forwarding"]
        index_project: "projects-github-imagetransformer (reindexed: 118 → 127 files / 288 symbols)"
        history: ".agents/context/history.md (entry 2026-09-17)"
        error_log: ".agents/context/error.log (local only: *.log is gitignored)"

    "2026-09-17_dependency-refresh":
      status: "completed"
      summary: "Dependency refresh (slices C1 + C2a) + Dependabot version-update enablement + review fixes ID-01/02/03, archived in 3 atomic commits (head 7d340af)."
      reviewer_hash: "73ad2513d2f50ce9"
      reviewed_at: "2026-09-17"
      scribe_hash: "rev:73ad2513d2f50ce9+spec:security-hardening.md#D+R16-R21"
      completed_at: "2026-09-17T16:29:24+01:00"
      completed_at_note: "Timestamp of that cycle's /scribe mem_save (no `date` allowed in the /scribe sandbox)."
      adr_file: ".agents/docs/adr/0005-dependency-advisory-policy.md"
      plan_file: ".agents/docs/plan_2026-09-17_dependency-refresh-and-advisory-closure.md"
      review_file: ".agents/docs/review_2026-09-17_dependency-refresh-dependabot-repass.md"
      review_prev_file: ".agents/docs/review_2026-09-17_dependency-refresh-dependabot.md"
      analysis_file: ".agents/docs/analysis_2026-09-17_security-advisory-triage.md"
      triage_file: ".agents/docs/evidence/advisory_triage_2026-09-17.md"
      dependabot_config: ".github/dependabot.yml"
      built_at: "2026-09-17"
      planned_at: "2026-09-17"
      build_hash: "slices C1+C2a + dependabot.yml + review fixes ID-01/02/03 @ 8a62fde — change_spec.yaml 2026-09-17-dependency-refresh-and-advisory-closure"
      diff_stat: "6 files modified + 4 new — backend/package.json 4+/4-, pnpm-workspace.yaml 6+/6-, pnpm-lock.yaml 59+/69-, .agents/*, .github/dependabot.yml (new, 86 lines), backend/src/__tests__/dependencyConfig.test.ts (new)"
      cycle_summary: "Slices C1 + C2a + Dependabot enablement DONE. Headline: pnpm audit full+prod = 0 and OSV 0/376 → no live advisory; the perceived 'security advisory problem' is 11 stale origin/dependabot/* refs (close-list emitted, none to merge). Refreshed: helmet 8.3.0, express-rate-limit 8.7.0, zod 4.6.2, tsx 4.23.13 + 6 override floors. NEW: .github/dependabot.yml (version updates). Verified: pnpm qa exit 0 (18 files/135 tests), pnpm build exit 0, audit 0/0, OSV 0/57, runtime smoke 200/200."
      archive:
        spec_merged_into: ".agents/docs/specs/security-hardening.md §D (R16..R21)"
        change_spec: ".agents/specs/change_spec.yaml (status: archived)"
        specs_readme: ".agents/specs/README.md (OpenSpec-equivalent flow)"
        adr: ".agents/docs/adr/0005-dependency-advisory-policy.md"
        memory_ids: [34, 35, 36]
        index_project: "projects-github-imagetransformer (118 files / 282 symbols, first index)"
        manifest_updated: ".agents/project_manifest.yaml (project.index_project + tooling/testing)"
      workload:
        slices: "C1 done | C2a done | C2b + C3 pending"
        note: "Slice C2a stayed inside the 5-file/400-line budget (4 files, 93 insertions). NEVER use `pnpm up -r` here: it produced a 1198-line lockfile diff and rewrote `catalog:`/exact pins. Use `pnpm install --no-frozen-lockfile`."
      previous_cycle:
        completed_at: "2026-09-10"
        summary: "Full security remediation: 2 HIGH XSS + sharp CVE + 13 Dependabot reports + deferred hardening (nginx rootless, trust-proxy, magic-bytes, digests). Reviewer APROBADO (40 KEEP/0 REMOVE). Suite 135/135, audit full+prod 0."
