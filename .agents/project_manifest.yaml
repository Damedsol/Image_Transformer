# ============================================================
# Project Manifest: imageTransformer
# Generated by: Initializer Agent
# Date: 2026-08-27
# ============================================================

project_name: "imageTransformer"
version: "2.0.0"
type: "monorepo (pnpm workspace v11)"
license: "CC BY 4.0"

tech_stack:
  - "TypeScript 6 (strict, isolatedModules, bundler mode)"
  - "Vite 8 (dev server, HMR, polling mode)"
  - "Vanilla Web Components (Custom Elements v1, Shadow DOM)"
  - "Vanilla CSS / CSS Custom Properties"
  - "Express 5 + Sharp 0.34 (backend REST API)"
  - "Iconoir Icons via <tn-icon> Web Component"
  - "Figtree (UI) + IBM Plex Mono (data/code)"
  - "WCAG 2.2 AAA"
  - "Neon-Code UI Kit / SNA"

tooling:
  package_manager: "pnpm v11"
  quality:
    - "Biome v2 (formatter)"
    - "Oxlint v1 (linter)"
    - "lint-filenames v1 (script: naming)"
    - "Husky v9 + lint-staged + commitlint"
  testing: "Vitest + JSDOM"
  docker: "Docker multi-stage (Node 24 Alpine + Nginx Alpine)"

relevant_folders:
  - "src/components/       # Web Components PascalCase"
  - "src/utils/            # Utilities camelCase"
  - "src/types/            # Type definitions"
  - "src/__tests__/        # TDD suites"
  - "assets/               # Vendored fonts (fonts/) + icon SVGs (icons/)"
  - "assets/icons/         # Iconoir SVGs (vendorized, imported via ?raw)"
  - "backend/src/          # Backend logic camelCase"
  - ".agents/              # Agentic harness (manifest, checkpoint, context, docs, skills)"

omitted_folders:
  - node_modules
  - backend/node_modules
  - dist/
  - .git/
  - .husky/

entrypoints:
  frontend: "index.html"
  backend: "backend/src/index.ts"

commands:
  dev: "pnpm dev"
  dev:frontend: "pnpm dev:frontend"
  dev:backend: "pnpm dev:backend"
  build: "pnpm build"
  build:frontend: "pnpm build:frontend"
  build:backend: "pnpm build:backend"
  qa: "pnpm qa"
  test: "pnpm test"
  lint: "pnpm lint"
  format: "pnpm format"
  format:check: "pnpm format:check"
  type-check: "pnpm type-check"

env_keys:
  - "PORT"
  - "NODE_ENV"
  - "API_URL"
  - "TEMP_DIR"
  - "MAX_FILE_SIZE"
  - "RATE_LIMIT_WINDOW_MS"
  - "RATE_LIMIT_MAX"
  - "CORS_ORIGIN"
  - "LOG_LEVEL"
  - "UPLOAD_DIR"

services:
  - "backend: Express API + Sharp image processing"
  - "frontend: Vite SPA, Nginx in production"

skills:
  local:
    - "code-quality (.agents/skills/)"
  global: "Detected in the environment; NOT listed by name in this manifest"

policies: "Mandatory use of .agents/ + root AGENTS.md"
